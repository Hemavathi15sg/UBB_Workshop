name: Production Release

on:
  workflow_dispatch:
    inputs:
      service:
        description: Service to release
        required: true
        type: choice
        options:
          - payments-api
          - claims-api
          - admin-portal

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Release gate placeholder for ${{ inputs.service }}"
