# Repository Navigation

Start with the owning service and its tests. Use `docs/runbooks` for operational context, `docs/adr` for architecture decisions, and `legacy-benefits-engine` only when a task explicitly touches legacy integration.
