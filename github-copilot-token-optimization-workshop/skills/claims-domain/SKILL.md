# Claims Domain Skill

Use this skill when a task specifically involves claim statuses, claim status labels, or claim status API behavior.

Key facts:
- Claim statuses are defined in `packages/shared-domain/src/ClaimStatus.ts`.
- Admin portal labels are in `apps/admin-portal/src/claims/statusLabels.ts`.
- Claims API status behavior is in `services/claims-api/src/services/ClaimStatusService.ts`.
